"use client";
import { FormEvent,useState } from "react";import { Button } from "@/components/ui/button";
export function TodoForm({onAdd}:{onAdd:(title:string)=>void}){const [value,setValue]=useState("");const [error,setError]=useState("");function submit(event:FormEvent){event.preventDefault();const title=value.trim();if(!title){setError("Write a task before adding it.");return;}if(title.length>120){setError("Keep the task under 120 characters.");return;}onAdd(title);setValue("");setError("");}return <form onSubmit={submit} className="flex flex-col gap-3 sm:flex-row" aria-label="Add a task"><div className="flex-1"><label htmlFor="new-task" className="sr-only">New task</label><input id="new-task" value={value} onChange={e=>setValue(e.target.value)} placeholder="What needs your attention?" aria-invalid={Boolean(error)} aria-describedby={error?"task-error":undefined} className="h-12 w-full rounded-xl border border-line bg-white px-4 outline-none focus:border-ink focus:ring-4 focus:ring-[#b9dfd166]"/>{error&&<p id="task-error" className="mt-2 text-sm text-[#b33f2d]">{error}</p>}</div><Button type="submit" className="h-12 sm:min-w-32">Add task <span aria-hidden>＋</span></Button></form>;}
